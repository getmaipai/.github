{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://getmaipai.github.io/.github/standards/schemas/budget.schema.json",
  "title": "Budget",
  "description": "One performance budget declaration, measured against a named reference machine. See platform plan 2.2 > Performance budgets. CI measures against these; the release skill re-runs the benches; the host faults a package that runs over its own timeout_ms budget at runtime (that check uses the package manifest's timeout_ms directly, not this file, which is for the budgets CI proves in aggregate: prompt assembly, page-open, cold shell, and similar).",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "metric", "threshold", "unit", "reference_machine"],
  "properties": {
    "id": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },
    "description": { "type": "string" },
    "metric": {
      "type": "string",
      "description": "What this budget bounds, e.g. first_token_ms, cold_shell_ms, page_open_ms, prompt_tokens, handle_timeout_ms, memory_mb."
    },
    "threshold": { "type": "number" },
    "unit": {
      "type": "string",
      "enum": ["ms", "tokens", "mb"]
    },
    "reference_machine": {
      "type": "string",
      "enum": ["msi_hub", "two_gen_laptop", "two_gen_iphone", "current_apple_tv", "bench_pi"],
      "description": "The named reference machines from 2.2: the MSI hub, a two-generation-old laptop, a two-generation-old iPhone, the current Apple TV, the bench Pi."
    }
  }
}
