# generated by datamodel-codegen:
#   filename:  trace-span.schema.json

from __future__ import annotations

from typing import Any, Literal

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, constr


class TraceSpan(BaseModel):
    """
    One span in a trace. One trace id runs from wake word through the router, a package, and an integration to the reply, shown as a timeline in Developer tools. See platform plan 2.1 and docs/ENGINEERING.md > Debugging and diagnostics.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    trace_id: constr(min_length=1)
    span_id: constr(min_length=1)
    parent_span_id: str | None = None
    name: str = Field(
        ...,
        description='e.g. wakeword.detect, router.route, plugin.handle:bedtime-reminder, integration.call:home_assistant.',
    )
    node: Literal['home', 'bot'] = Field(
        ...,
        description='Which node emitted this span. A cross-node call (5.3) gets a span on each side sharing the trace_id.',
    )
    start: AwareDatetime
    end: AwareDatetime | None = Field(
        None, description='Null while the span is still open.'
    )
    status: Literal['ok', 'error'] | None = 'ok'
    attributes: dict[str, Any] | None = {}
